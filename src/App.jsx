import SearchScene from './scenes/SearchScene.jsx'

// One visualizer, one screen. The old three-act nav is gone (the legacy scenes remain in the
// repo, unreferenced, until their keepers — overlay copy, click-to-name — are folded in).
export default function App() {
  return <SearchScene />
}
